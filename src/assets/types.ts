export interface NoteObj {
    title?: string,
    description?: string,
    imagePaths?: string,
    isPinned?: boolean,
    isTrashed?: boolean,
    isArchived?: boolean,
    color?: string,
    reminder?:Date,

    
}
