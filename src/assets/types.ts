export interface NoteObj {
    title?: string,
    description?: string,
    isArchived?: boolean,
    isDeleted?: boolean,
    service?: string,
    id?: string,
    IsColour?: string,
    reminder?:Date,
    EmailId?:string
}